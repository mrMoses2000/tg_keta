# Core async framework
aiohttp>=3.9,<4.0
# Supabase REST client (recipes, read-only queries)
supabase>=2.0,<3.0
# Local Postgres (state tables: conversation_state, inbound/outbound events, processed_updates)
asyncpg>=0.29,<1.0
# Redis (queue + cache + distributed locks)
redis>=5.0,<6.0
# Pydantic (models + settings + validation)
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
# Structured logging
structlog>=24.0
# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.0
aioresponses>=0.7  # mock aiohttp
